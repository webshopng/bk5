import type React from "react"
export const metadata = {
  title: "Download Blank Shell Script (.sh) File | Free Template",
  description:
    "Get a free, properly formatted blank shell script (.sh) file template. Download now for your shell scripting projects on Linux, macOS, or Unix systems.",
}

export default function Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}

